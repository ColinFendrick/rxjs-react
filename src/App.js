import React from 'react';
import { Container } from 'reactstrap';

export default () => {
	return (
		<Container fluid>
			<div>
				<header>
					<p>
            Playing around with rxjs in react
					</p>
				</header>
			</div>
		</Container>
	);
};
